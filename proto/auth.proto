syntax = "proto3";

package auth;

service AuthService {
    rpc Authenticate (Authentication) returns (UserMesage) {}
}

message Authentication {
    string Authenication = 1;
}

message UserMesage {
    string id = 1;
    string email = 2;
    string password = 3;
    repeated string roles = 4;
}